{
  "name": "@wallet/platform-api",
  "version": "0.1.0",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "tslint --project . 'index.ts'",
    "watch": "npm run build -- --watch",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/big.js": "^4.0.0"
  }
}
